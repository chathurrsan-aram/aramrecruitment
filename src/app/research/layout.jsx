export const metadata = {
  title: 'Research & Insights',
  description: 'Field observations and sector insights from across Sri Lanka, district by district.',
};

export default function Layout({ children }) {
  return children;
}
