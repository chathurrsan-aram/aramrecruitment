export const metadata = {
  title: 'Ideas Hub',
  description: 'Emerging initiative ideas for the 2026 trip (volunteers only).',
};

export default function Layout({ children }) {
  return children;
}
