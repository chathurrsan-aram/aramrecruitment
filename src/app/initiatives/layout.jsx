export const metadata = {
  title: { default: 'Initiatives', template: '%s | Aram' },
  description: 'Long-term projects in Sri Lanka: microcredit, pen pals, mentorship and the activities delivered on our trips.',
};

export default function Layout({ children }) {
  return children;
}
