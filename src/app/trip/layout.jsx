export const metadata = {
  title: 'The Trip',
  description: 'Our annual volunteering trip to Sri Lanka: how it works, past trips and what is next.',
};

export default function Layout({ children }) {
  return children;
}
